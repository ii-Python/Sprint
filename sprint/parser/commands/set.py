# Modules
from readchar import readkey

# Set function for changing environment variables
# within the runtime
def main(args, flags, core):
    core.logger.warn("NotImplemented", "The set command is still a work in progress.")
